# ============================================================================
# VARIABLES DE ENTORNO - IMAGEN BASE SYMFONY
# ============================================================================
# Copia este archivo como .env en tu proyecto y ajusta los valores según necesites

# ----------------------------------------------------------------------------
# DOCKER COMPOSE - Perfil activo
# ----------------------------------------------------------------------------
# Opciones válidas: "development" o "production"
COMPOSE_PROFILES=development

# ----------------------------------------------------------------------------
# DOCKER BUILD - Variables de construcción de imagen (build-time)
# ----------------------------------------------------------------------------
# Estas variables se usan durante docker build y son completamente opcionales
# Si no las defines, se usan los valores por defecto del Dockerfile

# ============================================================================
# VARIABLES DE ENTORNO - IMAGEN BASE SYMFONY
# ============================================================================
# Copia este archivo como .env en tu proyecto y ajusta los valores según necesites


# Versión de PHP a instalar (disponibles: 8.0, 8.1, 8.2, 8.3, 8.4)
PHP_VERSION=8.3

# Locale del sistema operativo
LOCALE=es_ES.UTF-8

# Zona horaria del contenedor
TIMEZONE=America/Havana

# ----------------------------------------------------------------------------
# DOCKER - Usuario y grupo para el contenedor (solo desarrollo)
# ----------------------------------------------------------------------------
# Estos valores deben coincidir con tu usuario del host para evitar problemas de permisos
UID=1000
GID=1000

# ----------------------------------------------------------------------------
# BASE DE DATOS - PostgreSQL
# ----------------------------------------------------------------------------
DB_HOST=database
DB_PORT=5432
DB_DATABASE=myapp
DB_USER=myapp_user
DB_PASSWORD=ChangeThisSecurePassword

# ----------------------------------------------------------------------------
# SYMFONY - Configuración básica
# ----------------------------------------------------------------------------
# Valores: dev | prod | test
APP_ENV=dev
# Valores: 0 (false) | 1 (true)
APP_DEBUG=1
# Genera un secreto único: php -r "echo bin2hex(random_bytes(16));"
APP_SECRET=CHANGE_THIS_SECRET_KEY_32_CHARS

# ----------------------------------------------------------------------------
# DOCTRINE - Database URL
# ----------------------------------------------------------------------------
# Esta variable usa las variables DB_* definidas arriba
DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_DATABASE}?serverVersion=16&charset=utf8"

# Alternativas (descomenta la que necesites):
# SQLite (para desarrollo rápido sin BD externa)
# DATABASE_URL="sqlite:///%kernel.project_dir%/var/data.db"

# MySQL/MariaDB
# DATABASE_URL="mysql://user:password@127.0.0.1:3306/dbname?serverVersion=8.0&charset=utf8mb4"

# ----------------------------------------------------------------------------
# MAILER - Configuración de correo
# ----------------------------------------------------------------------------
# En desarrollo, usa Mailpit (incluido en docker-compose)
MAILER_DSN=smtp://mailpit:1025

# Para producción, usa un servidor SMTP real:
# MAILER_DSN=smtp://usuario:contraseña@smtp.example.com:587
# MAILER_DSN=gmail+smtp://usuario:contraseña@default
# MAILER_DSN=sendgrid+smtp://apikey:tu-api-key@default

